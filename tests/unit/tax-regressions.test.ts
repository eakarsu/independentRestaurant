import test from 'node:test';import assert from 'node:assert/strict';import {ConfiguredTaxProvider} from '../../src/lib/commerce/providers';
test('blank tax configuration cannot silently become zero tax',async()=>{const previous=process.env.TAX_RATE_BPS;const provider=new ConfiguredTaxProvider();const input={idempotencyKey:'test',currency:'USD',subtotalCents:1999,discountCents:0,lines:[]};try{for(const value of ['', ' ', 'NaN']){process.env.TAX_RATE_BPS=value;await assert.rejects(provider.quote(input));}process.env.TAX_RATE_BPS='0';assert.equal((await provider.quote(input)).taxCents,0);process.env.TAX_RATE_BPS='500';assert.equal((await provider.quote(input)).taxCents,100);await assert.rejects(provider.quote({...input,discountCents:2000}));}finally{if(previous===undefined)delete process.env.TAX_RATE_BPS;else process.env.TAX_RATE_BPS=previous;}});
